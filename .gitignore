# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Backup files
*.bak
*.backup
*.old

# OS
.DS_Store
Thumbs.db

# Application specific
*.db
*.db-journal
*.db-shm
*.db-wal
*.log

# Per-machine config + updater state — MUST stay local, never distributed
# (each PC configures its own mode/paths via the setup wizard).
config/config.json
.str_version
logs/

# Temporary files
*.tmp
temp/
tmp/

# Compiled executables
*.exe
*.dll
*.so
*.dylib
issues/nul
nukebox/
nukebox_run.log
uibox/
EXPORT_DIR/
nuke.db

# Test scratch boxes (generated by tests_e2e_harness / tests_prosecutor)
pbox/
sandbox/

# graphify knowledge graph output (generated)
graphify-out/

.playwright-mcp/

# Bank data and scratch boxes. An autonomous loop with commit rights must not
# be able to stage a report database, an export, or a sandbox run.
cbox/
nukebox/
uibox/
pbox/
sandbox/
*.xlsx
*.xls
*.csv
*.db
*.sqlite
*.sqlite3

# Loop scratch state (gate output). The LEDGER is tracked; this is not.
.loop/

# Per-machine agent permissions. Contains local paths and is not shared state.
.claude/settings.local.json
